{
  "name": "palette-test",
  "displayName": "Palette Test",
  "description": "Minimal test extension",
  "version": "0.1.0",
  "engines": {
    "vscode": "^1.60.0"
  },
  "categories": ["Other"],
  "main": "./out/test-minimal.js",
  "activationEvents": [
    "onCommand:palette.test",
    "onCommand:palette.openUnified"
  ],
  "contributes": {
    "commands": [
      {
        "command": "palette.test",
        "title": "🧪 Palette Test"
      },
      {
        "command": "palette.openUnified",
        "title": "🎨 Palette: Open Unified System (Test)"
      }
    ]
  },
  "scripts": {
    "compile": "tsc -p .",
    "watch": "tsc -w -p ."
  },
  "devDependencies": {
    "@types/node": "16.x",
    "@types/vscode": "^1.60.0",
    "typescript": "^4.9.4"
  }
}